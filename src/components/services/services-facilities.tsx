import Section from "../ui/section";

export default function ServicesFacilities() {
    return (
        <Section size={"screen"}>
            <Section.Inner className="flex flex-col"></Section.Inner>
        </Section>
    );
}
